# 📖 ConsultationHub 사용자 매뉴얼 & 가이드

본 매뉴얼은 **ConsultationHub (일일 상담 관리 장부)** 프로그램의 화면 구성 및 주요 기능 활용법을 안내합니다.

---

## 🖥️ 1. 화면 구성 (3단 컬럼 레이아웃)

프로그램 화면은 효율적인 업무 처리를 위해 **3개 구역**으로 나뉘어져 있습니다.

```text
┌──────────────────┬─────────────────────────────────────┬──────────────────────────┐
│  🔍 검색 및 필터  │  📝 상담 작성 및 상세 내용 (중앙)    │  📋 상담 기록 목록        │
│  (좌측 250px)    │  - 핵심 고객 정보(성함/연락처/주소)  │  (우측 520px)            │
│  - 통합 키워드   │  - 💡 실시간 자동 힌트 배너          │  - 총 건수 뱃지          │
│  - 카테고리/상태 │  - 상담 요약 및 상세 메모           │  - 1,2,3 순번 (No.)      │
│  - 날짜 범위     │  - 💾 저장 / 신규 작성 / 🗑 삭제      │  - 항목 선택 시 즉시 바인딩│
└──────────────────┴─────────────────────────────────────┴──────────────────────────┘
```

---

## 📝 2. 신규 상담 작성 방법

1. **상담 입력 구역 확인**: 화면 중앙의 **[👤 핵심 고객 정보]** 영역으로 이동합니다.
2. **핵심 인적사항 입력**:
   - **연락처 (전화번호) [필수]**: 고객의 휴대폰 번호 또는 일반 전화를 입력합니다.
   - **주소 (소재지/배송지) [필수]**: 소재지, 배송지 또는 상담 장소를 입력합니다.
   - **고객명 (성함) [선택]**: 고객의 이름을 입력합니다. (미기재 시 '(미기재)'로 자동 표시)
3. **⚡ 실시간 기존 고객 자동 감지 힌트**:
   - 성함, 연락처, 주소를 입력하는 순간, 기존 데이터베이스에 동일하거나 유사한 고객 정보가 보관되어 있다면 아래와 같은 파란색 힌트 배너가 실시간으로 노출됩니다.
     > `💡 힌트: '김철수' 님과 일치하는 이전 상담 3건 보관 중 (최근: 2026-08-30 14:00)`
   - **[기존정보 채우기]** 버튼: 기존 고객의 성함/연락처/주소가 폼으로 즉시 입력됩니다.
   - **[이력 보기]** 버튼: 해당 고객의 과거 상담 기록만 오른쪽 목록에 필터링되어 빠르게 대조할 수 있습니다.
4. **상담 상세 내용 입력 및 🔍 글자 크기 조절**:
   - 카테고리 (일반상담, 법률/행정, 상품문의, 서비스지원, 기타) 선택
   - 진행 상태 (대기중, 진행중, 완료, 보류) 선택
   - 우선순위 (보통, 중요, 긴급) 선택
   - 재상담(팔로우업) 예정일 지정
   - **상담 요약 (한 줄 정리) [필수]** 및 태그 (`#신규고객,#견적서` 등) 입력
   - **상세 상담 내용** 기록
   - **🔍 글자 크기 자유 조절**:
     - `A-` / `A+` 버튼 클릭으로 1pt씩 미세 조절
     - `[14pt ▼]` 드롭다운에서 10pt ~ 28pt 원하는 크기 즉시 선택
     - `초기화` 버튼 클릭 시 기본 크기(14pt)로 복구
     - **단축키**: 입력창 위에서 **`Ctrl + 마우스 휠`** 스크롤로 즉시 확대/축소 가능
     - 설정된 글자 크기는 다음 실행 시에도 **자동으로 기억되어 유지**됩니다.
5. **저장하기**: 맨 아래 `💾 상담 내용 저장하기` 버튼을 누르면 즉시 DB에 보관됩니다.

---

## 🔍 3. 기존 상담 조회 및 검색

- **통합 키워드 검색**: 좌측 검색창에 고객명, 연락처, 주소, 상담 내용, 태그 키워드를 타자치면 **0.1초 만에 즉시 검색**됩니다.
- **조건별 필터**: 카테고리, 진행 상태, 시작일~종료일을 조합하여 특정 조건의 상담만 찾아볼 수 있습니다.
- **필터 초기화**: `필터 초기화` 버튼을 누르면 검색 조건이 리셋되어 전체 목록이 다시 표시됩니다.

---

## ✏️ 4. 기존 상담 수정 및 삭제

1. **수정하기**: Right (우측) 목록에서 수정하고 싶은 상담 건을 클릭합니다.
2. 클릭하는 즉시 중앙 편집 폼으로 데이터가 불러와지며, 내용을 수정한 후 `💾 상담 내용 저장하기` 버튼을 누르면 업데이트됩니다.
3. **삭제하기**: 수정을 취소하거나 제거하고 싶은 항목을 선택 후 `🗑 삭제` 버튼을 누르면 확인창 후 안전하게 삭제됩니다. (삭제 시 전체 건수가 바로 차감되고, 목록의 순번(No.)이 1, 2, 3...으로 예쁘게 재정렬됩니다.)

---

## 📊 5. 엑셀 (CSV) 파일 내보내기

- 좌측 메뉴의 **`📊 CSV 파일 내보내기`** 버튼을 클릭합니다.
- 현재 검색된 상담 기록들이 엑셀 호환 파일(`상담장부_내보내기_YYYYMMDD_HHMMSS.csv`)로 **바탕화면에 자동 저장**됩니다.

---

## 🔒 6. 데이터 보안 및 백업 위치

- 본 프로그램은 외부 인터넷 서버로 고객 정보를 전송하지 않고, **사용자 로컬 컴퓨터 내부**에 보관됩니다.
- **DB 파일 위치**: `C:\Users\grayc\AppData\Roaming\ConsultationLedger\consultations.db`
- 해당 `consultations.db` 파일을 복사해 두면 언제든지 간편하게 데이터 전체를 백업하거나 다른 컴퓨터로 이전할 수 있습니다.
